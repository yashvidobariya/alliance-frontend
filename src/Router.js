const baseURL = "https://alliance-backend.vercel.app";

const Formdata = `${baseURL}/form/addData`;
const account = `${baseURL}/account/searchAccounts`;

module.exports = {
    Formdata,
    account
};